[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "pip install yt-dlp yt-dlp-ejs --break-system-packages || pip install yt-dlp yt-dlp-ejs",
  "curl -fsSL https://deno.land/install.sh | sh",
  "cp /root/.deno/bin/deno /usr/local/bin/deno"
]

[start]
cmd = "node server.js"